# Server Bootstrap Scripts

Набор bash-скриптов для **быстрой и воспроизводимой настройки Ubuntu-сервера** под проекты на **Laravel + PHP 8.2**.

Цель проекта — убрать рутину ручной настройки сервера и получить предсказуемый результат за несколько минут.

---

## 🧩 Что решает этот набор скриптов

* установка и базовая настройка PHP 8.2
* установка Composer
* установка и настройка MySQL с созданием БД и пользователя
* установка Laravel и первичная конфигурация `.env`
* установка и настройка Nginx под Laravel
* подключение SSL через Let’s Encrypt (Certbot)
* настройка запуска задач по расписанию (Laravel Scheduler)

Скрипты можно запускать **по отдельности** или **последовательно**, в зависимости от задачи.

---

## 📁 Структура

```text
A_php.sh         # Установка PHP 8.2 и расширений
B_composer.sh    # Установка Composer
C_mysql.sh       # Установка MySQL + создание БД и пользователя
D_laravel.sh     # Установка Laravel проекта
E_nginx.sh       # Установка и настройка Nginx под Laravel
F_certbot.sh     # Установка SSL через Certbot
G_scheduler.sh   # Настройка cron для Laravel Scheduler
```

---

## 🧪 Поддерживаемый стек

* **OS**: Ubuntu 20.04 / 22.04
* **PHP**: 8.2 (php-fpm)
* **Web server**: Nginx
* **DB**: MySQL
* **Framework**: Laravel

---

## ▶️ Общие требования

* запуск от `root`
* доступ в интернет
* чистый или минимально настроенный сервер

Перед запуском любого скрипта:

```bash
chmod +x *.sh
```

---

## 🛠 Описание скриптов

### `A_php.sh`

Устанавливает PHP 8.2 и основные расширения, необходимые для Laravel.

Что делает:

* добавляет PPA `ondrej/php`
* устанавливает `php-cli`, `php-fpm`, `mbstring`, `pdo`, `mysql`, `xml`, `curl`, `zip` и др.
* делает PHP 8.2 версией по умолчанию

---

### `B_composer.sh`

Устанавливает Composer глобально.

Особенности:

* проверка подписи installer
* установка в `/usr/local/bin/composer`
* безопасен для повторного запуска

---

### `C_mysql.sh`

Устанавливает MySQL Server и создаёт базу данных.

Во время выполнения **интерактивно запрашивает**:

* имя базы данных
* имя пользователя
* пароль

После этого:

* создаёт БД
* создаёт пользователя
* выдаёт все привилегии на эту БД

---

### `D_laravel.sh`

Создаёт новый Laravel-проект.

Скрипт:

* проверяет наличие PHP 8.2 и Composer
* спрашивает путь установки проекта
* создаёт Laravel через `composer create-project`
* запрашивает данные БД
* автоматически заполняет `.env`
* генерирует `APP_KEY`

---

### `E_nginx.sh`

Устанавливает и настраивает Nginx под Laravel.

Скрипт:

* спрашивает путь к Laravel-проекту
* спрашивает домен (можно оставить пустым → доступ по IP)
* создаёт nginx server block
* настраивает `php-fpm 8.2`
* корректно выставляет `chown` и `chmod`

---

### `F_certbot.sh`

Устанавливает Certbot и выпускает SSL-сертификат Let’s Encrypt.

Используется после настройки Nginx.

---

### `G_scheduler.sh`

Настраивает cron для Laravel Scheduler.

Добавляет стандартную задачу:

```bash
* * * * * php artisan schedule:run
```

---

## 🔁 Типичный порядок запуска

```text
A_php.sh
B_composer.sh
C_mysql.sh
D_laravel.sh
E_nginx.sh
F_certbot.sh
G_scheduler.sh
```

---

## ⚠️ Важно

* Скрипты ориентированы на **один проект на сервер**
* Не предназначены для shared-хостинга
* Не заменяют полноценный configuration management (Ansible и т.п.)

---

## 🎯 Зачем это нужно

* ускорить развёртывание новых проектов
* избежать ошибок ручной настройки
* получить одинаковую конфигурацию серверов
* иметь прозрачный и расширяемый bootstrap
